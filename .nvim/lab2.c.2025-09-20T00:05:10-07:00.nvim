#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main() {
  char input[50];

  printf("Enter program to run.");
  fgets(input, sizeof(input), stdin);  
  
  char *args[] = {"/", input, NULL};
  execv("/", args);
} 
