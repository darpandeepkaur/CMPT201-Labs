#define _POSICX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <vector>

std::vector<std::string> stringArray;
void input_history() {
  
}

int main() {
  char *line = NULL;
  size_t len = 0;

  printf("Please enter sone text:");

  ssize_t input = getline(&line, &len, stdin);

  if (input == -1) {
    perror("getline failed");
    free(line);
    exit(EXIT_FAILURE);
  }


}
