#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>

int main() {
  /*char *saveptr;*/
  /*char *ret = strtok_r(string, " ", &saveptr);*/
  /*char *ret2 = strtok_r(NULL, " ", &saveptr);*/

  char *line = NULL;
  char len = 0;
  ssize_t input = getline(&line, &len, stdin);

  printf("Enter a line:");

  if (input == -1) {
    perror = "getline failed";
    exit(EXIT_FAILURE);
  } else {
    printf("You entered: %s", line);
  }
  free(line);
  return 0;
}
